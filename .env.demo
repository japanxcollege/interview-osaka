# ================================
# Interview Osaka - Demo Variables
# ================================
# このファイルは開発/デモ用テンプレートです。
# 実際のキーはコミットせず、手元の `.env` / `.env.local` などにコピーして使ってください。

# ---------- Backend (FastAPI) ----------
OPENAI_API_KEY=your_openai_api_key_here        # 必須: Whisper利用時
FRONTEND_URL=http://localhost:3000             # CORS許可先
WHISPER_MODEL=gpt-4o-mini-transcribe           # 任意: 使用モデル(未指定でこの値)
WHISPER_LANGUAGE=ja                            # 任意: 想定言語(例: ja, en)
GEMINI_API_KEY=your_gemini_api_key_here        # 任意: Gemini機能を使う場合のみ

# ---------- Frontend (Next.js) ----------
NEXT_PUBLIC_API_URL=http://localhost:8000      # APIのURL
NEXT_PUBLIC_WS_URL=ws://localhost:8000         # WebSocketのURL